@model IEnumerable<KindergartenApp.Models.Class_Trip>

@{
    ViewBag.Title = "Classes-Trip Manager";
}

<h2>Classes-Trip Manager</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Class.ClassTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trip.TripTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Class.ClassTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trip.TripTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
